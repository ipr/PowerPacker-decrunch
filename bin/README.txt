
Simple tool to unpack powerpacker-files, use as:
ppunpack.exe <infile> [outfile]

If only input-file is given it is overwritten, otherwise writes to given output file.

You'll need C/C++ runtime libraries from Microsoft too (Visual C++ 2010) to run it.. Quick googling should find it.

